name: Deploy preview

on:
  pull_request:

env:
  IS_PROD: false

permissions:
  checks: write
  contents: read
  deployments: write
  pages: write
  id-token: write

jobs:
  deploy:
    if: ${{ github.repository == 'primer/doctocat-nextjs' }}
    name: Preview
    # SHA for security hardening. Points at last verified HEAD of main branch.
    uses: primer/.github/.github/workflows/deploy_preview.yml@rezrah/fix-preview-deploy
    with:
      node_version: 18
      install: npm ci
      build: npm run build
      output_dir: packages/site/out
