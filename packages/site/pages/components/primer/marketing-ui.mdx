---
title: Marketing UI components
description: Learn how to use Primer Brand components in Markdown files.
show-tabs: true
---

import {Button, CTABanner, Stack, Pillar, ButtonGroup} from '@primer/react-brand'
import {GitBranchIcon, RocketIcon, CopilotIcon} from '@primer/octicons-react'

Primer Brand is a GitHub’s design system for creating marketing websites and digital experiences.

Marketing React components can be used in Markdown files by importing them from `@primer/react-brand`.

[Refer to the Primer Brand documentation](https://primer.style/brand) for more information on which components are available and how to use them.

## Using Primer Brand in Markdown files

1. Import a component from `@primer/react-brand`:

```js
import {Button} from '@primer/react-brand'
```

2. Use the component in your Markdown file, but ensure that you wrap the component in a `div` with the `custom-component` class. This will ensure that article formatting isn't applied to the custom component.

```jsx
<div class="custom-component">
  <ButtonGroup>
    <Button>Primary</Button>
    <Button>Secondary</Button>
  </ButtonGroup>
</div>
```

Will render:

<br />

<div class="custom-component">
  <ButtonGroup>
    <Button>Primary</Button>
    <Button>Secondary</Button>
  </ButtonGroup>
</div>

### Layout

Use [Stack](https://primer.style/brand/components/Stack), [Grid](https://primer.style/brand/components/Grid) and [Box](https://primer.style/brand/components/Box) components to create custom layouts.

Example using Stack and Pillars:

<br />

<div className="custom-component">
  <Stack direction="horizontal" alignItems="center" justifyContent="center" gap="spacious" padding="none">
    <Pillar>
      <Pillar.Icon icon={<CopilotIcon />} color="blue" />
      <Pillar.Heading>Heading</Pillar.Heading>
      <Pillar.Description>Everything you need to know about getting started with GitHub Actions.</Pillar.Description>
    </Pillar>
    <Pillar>
      <Pillar.Icon icon={<RocketIcon />} color="blue" />
      <Pillar.Heading>Heading</Pillar.Heading>
      <Pillar.Description>Everything you need to know about getting started with GitHub Actions.</Pillar.Description>
    </Pillar>
    <Pillar>
      <Pillar.Icon icon={<GitBranchIcon />} color="blue" />
      <Pillar.Heading>Heading</Pillar.Heading>
      <Pillar.Description>Everything you need to know about getting started with GitHub Actions.</Pillar.Description>
    </Pillar>
  </Stack>
</div>
